import { AdminConsole } from "@/components/admin/AdminConsole";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Hydroglide Admin | Operations Console"
};

export default function AdminPage() {
  return <AdminConsole />;
}
